#include<bits/stdc++.h>
using namespace std;

int main()
{
    int a,arr[5];
    int sum=0;

    for(a=0;a<5;a++)
    {
        cin>>arr[a];
        //sum+=arr[a];
        //cout<<arr[a]<<endl;
    }
    //
    //cout<<arr[a]<<endl;
     for(a=0;a<5;a++)
    {
        //cin>>arr[a];
        //sum+=arr[a];
        cout<<arr[a]<<endl;
    }

    return 0;
}
